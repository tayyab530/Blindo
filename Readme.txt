Install the following packages:

-> pip3 install torch torchvision torchaudio
-> pip install librosa
-> pip install pandas
-> pip install matplotlib
-> python3 -m pip install --upgrade Pillow
-> pip install -U scikit-learn


Run:
The program can be from the block of code after Run tag

=> input two digits spokenly, one by one
=> when done cancel the window
=> the other digit window will pop up...
=> at the end the dialog will appear with correct or incorrect password